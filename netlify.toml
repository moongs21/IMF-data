[build]
  # 빌드 명령어 (필요 없음 - 정적 파일만 배포)
  command = ""
  # 배포할 디렉토리
  publish = "."

[build.environment]
  # Node.js 버전 (Netlify Functions에 필요)
  NODE_VERSION = "18"

[[redirects]]
  # SPA 라우팅을 위한 리다이렉트 (필요한 경우)
  from = "/*"
  to = "/index.html"
  status = 200

